## Map
- Key 와 Value 로 구성되어 있다.
- 키는 맵에서 유일해야 한다. 그리고 키를 통해 값을 빠르게 검색할 수 있다.
- 순서를 보장하지 않는다.

### HashMap
- 입력한 순서를 보장하지 않는다.

### Linked HashMap
- 키를 기준으로 입력한 순서를 보장한다.

### TreeMap
- 키의 값을 기준으로 정렬한다.