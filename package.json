{
  "name": "study-notes",
  "private": true,
  "scripts": {
    "lint": "markdownlint-cli2 \"docs/**/*.md\"",
    "lint:links": "find docs -name '*.md' | xargs -I {} markdown-link-check {}"
  },
  "devDependencies": {
    "markdownlint-cli2": "^0.14.0",
    "markdown-link-check": "^3.12.0"
  }
}
