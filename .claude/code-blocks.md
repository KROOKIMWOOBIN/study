## 코드 블록 언어 표시

`docs/` 하위 마크다운 파일의 코드 블록은 언어에 관계없이 **항상 `markdown`** 으로 표시한다. 새로 작성할 때도, 기존 파일을 수정할 때도 동일하게 적용한다.

```
\```markdown
// 코드 예시
\```
```

**이유:** IDE에서 `java`, `text` 등 언어명 지정 시 빨간 줄(lint)이 떠서 불편함.
**주의:** `java`, `python`, `sql`, `text`, `bash` 등 어떤 언어명도 사용하지 말 것.
